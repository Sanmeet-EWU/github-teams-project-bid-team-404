---
interface Props {
  date: Date | undefined;
}

const { date } = Astro.props;
---

<time datetime={date ? date.toISOString() : ''}>
  {date
    ? date.toLocaleDateString('en-us', {
        year: 'numeric',
        month: 'short',
        day: 'numeric',
      })
    : 'Invalid Date'}
</time>
